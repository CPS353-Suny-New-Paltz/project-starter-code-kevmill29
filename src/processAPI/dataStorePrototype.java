package processAPI;

import java.util.ArrayList;
import java.util.HashMap;

public class dataStorePrototype {
//store bytes for now
	public void prototype(dataStore store) {
		//Data Storage only needs one thing for now a method that allows you to read the data currently stored
		//File file = new File("Output/output.txt");
		//"void method" readData : will return to console the data current stored in the output file
		HashMap<Integer, String> map = new HashMap<>();//ConvertData.convertData();
		//checks if map is empty if not sends insert request to the dataStore
		if(!map.isEmpty()) {
			
		}
		
	}
}
